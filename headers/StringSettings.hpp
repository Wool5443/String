#pragma once

#include <string.h>

static const size_t DEFAULT_STRING_CAPACITY = 8;
static const size_t STRING_GROW_FACTOR      = 2;

static const size_t MAX_CAPACITY            = 128 * 1024 * 1024; // 128 MB
